# HandSign Conversion - Python dependencies
#
# Python 3.10-3.12 (MediaPipe has no 3.13+ wheels yet).
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)   |   source .venv/bin/activate
#   pip install -r requirements.txt
#
# The previous version of this file was a full `pip freeze` of an Ubuntu
# desktop (Brlapi, cups, yt-dlp, ...) and did not contain a single package the
# project actually imports. This list is exactly what the code uses.

# --- Machine learning -------------------------------------------------
# scikit-learn is pinned to the version models/sign_model.pkl was trained with;
# loading a pickle with a different minor version is not guaranteed to work.
scikit-learn==1.9.0
joblib==1.5.3
numpy==1.26.4              # mediapipe 0.10.x requires numpy < 2

# --- Vision -----------------------------------------------------------
mediapipe==0.10.21         # hand landmark detection
opencv-python==4.11.0.86   # image decode, camera capture for the CLI tools

# --- Backend ----------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.3  # brings websockets + httptools
pydantic==2.13.4
pydantic-settings==2.15.0  # reads .env

# --- Language model ---------------------------------------------------
langchain-core==1.5.4              # prompt templates + structured output
langchain-google-genai==4.3.3      # Gemini chat model

# --- Data tooling (training and dataset scripts only) -----------------
pandas==3.0.5

# --- Tests ------------------------------------------------------------
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1              # required by fastapi.testclient
